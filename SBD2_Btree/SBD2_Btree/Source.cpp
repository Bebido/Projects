#include "Header.h"

//int max = 5;

int main()
{
	srand(time(NULL));
	/*rekord tmp;
	tmp.klucz = 1;
	tmp.generuj();
	cout << tmp.a << " " << tmp.b << " " << tmp.suma << endl;*/
	/*int tab[5] = { 0, 1, 2, 3, 4 };
	int n = 3;
	int tmp;

	for (int i = n, k = 0; k < 15; ++k, ++i)
	{
		if (i < 0)
			i = (max -1);
		else if (i > (max - 1))
			i = 0;
		tmp = i;
		cout << tab[tmp] << endl;
	}*/
	system("pause");
	return 0;
}